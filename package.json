{
  "name": "commonnote-independent-status",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": { "test": "node --test tests/*.test.mjs", "build": "node scripts/build-status.mjs _status_dist" }
}
